# 工业环境交互型智能体赛题技术路线

本文记录针对根目录赛题
`XH-202607_工业环境下物体感知识别与指令交互型智能体研发.pdf`
形成的阶段性技术安排。

## 1. 赛题核心目标

系统需要完成以下闭环：

```text
自然语言指令
→ 工业物体识别与三维定位
→ 指令理解
→ 作业任务序列分解
→ 机械臂抓取与放置
→ 视觉验证
→ 失败重试
```

典型任务：

```text
“帮我把滚柱放到料箱的第三个格子中”
```

不能只生成文字形式的任务序列。答题要求明确提出识别、抓取、放置以及
摆放失败后的自主检测和重新处理。

## 2. 评分导向

| 评审方向 | 分值 | 项目重点 |
| --- | ---: | --- |
| 方案创新性与完整性 | 35 | 完整覆盖感知、决策、执行，并体现复用性 |
| 技术实现质量 | 35 | 视觉精度、任务合理性、执行成功率、工业模型微调 |
| 系统集成与工程可用性 | 20 | 可复现仿真、实机扩展、真实机械臂与相机案例 |
| 文档与展示 | 10 | 技术报告、实验数据和演示视频 |

当前优先级应为：

```text
完整闭环
> 视觉微调和量化指标
> 实机验证
> 失败恢复
> 强化学习扩展
```

强化学习不是硬性要求，不应阻塞基础闭环。

## 3. 当前项目能力

### 已具备

- Agent、Tool、Skill、ActionList 和 DecisionTree 运行框架；
- 静态规划器和 OpenAI 兼容的 LLM 规划器；
- Silero VAD、SenseVoice ASR 和本地 TTS 文件生成；
- RM65-B 与 Robotiq 2F-85 组合模型；
- Gazebo Sim、MoveIt 2、`ros2_control`；
- 机械臂轨迹控制；
- 双指独立 effort 控制；
- 标准 `GripperCommand` Action 接口；
- Ubuntu 下机械臂运动和夹爪开合验证。

### 尚缺

- 工业 RGB-D 视觉识别与三维定位；
- 工业工具数据集和模型微调；
- Agent 到 ROS 2 的真实 robot Tool；
- 工业 Gazebo 工作台、料箱和混杂物体场景；
- 自动场景重置与批量评测；
- 抓取和放置后的视觉验证；
- 真实失败恢复工作流；
- 实机相机标定和完整闭环；
- Gymnasium 强化学习环境。

### 当前关键边界

```text
本地语音链 → Agent → mock.pick_place_actionlist

MoveIt / ROS 2 Action → RM65-B + Robotiq 仿真
```

两条链目前可以独立运行，但尚未连接。

## 4. 系统闭环与模块解耦

系统保持以下闭环：

```text
自然语言输入
→ 指令理解
→ 环境感知
→ 世界状态
→ 任务分解
→ 机器人执行
→ 结果验证
→ 失败恢复或任务完成
```

各模块通过稳定的输入输出接口协作，不应直接依赖其他模块内部实现。
低层轨迹规划、碰撞检查和安全限制继续由 MoveIt 2 与机器人运行时负责；
Agent 负责理解任务、选择能力、组织流程和处理执行结果。

## 5. 统一完成度定义

所有模块统一使用 L0～L3 四级完成度，避免使用“做了”“基本完成”等难以验收的
描述。

| 等级 | 名称 | 定义 |
| --- | --- | --- |
| L0 | 骨架 | 只有目录、接口草案、演示占位或概念验证 |
| L1 | 可运行 | 单模块能够在理想条件下跑通一个固定案例 |
| L2 | 可集成 | 接口稳定，可进入系统闭环，具有基础异常处理和测试 |
| L3 | 有竞争力 | 有量化指标、批量实验、复杂场景、失败分析和对比结果 |

比赛主链模块至少达到 L2，直接对应评分项的核心模块应达到 L3。

### 5.1 模块通用验收材料

模块达到 L2 或 L3 时，原则上应同时具备：

```text
明确的职责边界
稳定的输入输出接口
独立运行示例
配置和依赖说明
异常与失败处理
自动化或可重复测试
量化指标
典型成功和失败案例
系统集成方式
演示与报告素材
```

### 5.2 任务安排字段

后续拆分任务时，每个模块至少记录：

```text
负责人
当前等级
目标等级
输入依赖
输出接口
验收指标
截止时间
风险与备选方案
```

## 6. 模块总览

| 模块 | 核心职责 | 当前等级 | 目标等级 | 评分价值 |
| --- | --- | ---: | ---: | --- |
| 工业场景与数据 | 工具、零件、料箱、工作台、仿真和实机数据 | L0 | L3 | 高 |
| 视觉感知与定位 | 类别识别、实例区分、三维定位 | L0 | L3 | 极高 |
| 语音与指令交互 | VAD、ASR、指令结构化、交互反馈 | L1～L2 | L2 | 中 |
| 世界状态表达 | 统一表达物体、坐标、机器人和任务状态 | L0 | L2 | 高 |
| Agent 与任务分解 | 指令到受约束任务序列和执行分支 | L1 | L3 | 极高 |
| 机器人执行 | MoveIt、夹爪、抓取和放置能力接口 | L1～L2 | L3 | 高 |
| 结果验证与恢复 | 抓取/放置验证、失败分类和重试 | L0 | L3 | 极高 |
| 系统集成与运行 | 通信、配置、日志、一键启动、批量评测 | L1 | L3 | 高 |
| 仿真到实机 | 标定、安全、接口复用和真实案例 | L0～L1 | L3 | 极高 |
| 模型训练与实验 | 微调、对比、消融、指标和失败分析 | L0 | L3 | 极高 |
| 强化学习 | 策略优化，不阻塞基础闭环 | L0 | L1～L2 | 可选加分 |
| 文档与展示 | 报告、视频、说明和实验材料 | L1 | L3 | 必须 |

当前等级是阶段性判断，应在实际验收后调整。

## 7. 各模块目标完成度

### 7.1 工业场景与数据

**职责：**为视觉训练、仿真验证、实机实验和强化学习提供统一的任务环境。

**L1：**

- 有工作台、单个工业物体和料箱；
- 可以人工生成场景并完成一次观察；
- 有少量仿真或真实图像。

**L2：**

- 支持多类工具和零件；
- 支持规则摆放、散乱摆放和基础遮挡；
- 场景可以重复生成；
- 数据具有类别、实例和位置标注；
- 仿真与实机类别定义一致。

**L3：**

- 具有仿真和真实 RGB-D 数据；
- 支持位置、朝向、光照、背景和遮挡随机化；
- 数据集划分、生成和标注流程可复现；
- 包含复杂混杂场景和代表性失败场景；
- 可服务模型训练、系统测试和批量评测。

### 7.2 视觉感知与定位

**职责：**回答“场景里有什么、目标是哪一个、它在什么位置”。

**L1：**

- 能识别一个固定类别；
- 能在理想环境中输出二维检测结果。

**L2：**

- 支持多个工业类别和同类实例；
- 能结合深度信息输出三维位置；
- 能转换到统一机器人坐标系；
- 输出置信度并处理无目标、深度缺失等异常。

**L3：**

- 在混杂、遮挡和不同光照条件下保持竞争力；
- 有 mAP、FPS、三维定位误差和召回率；
- 完成工业数据微调，并提供微调前后对比；
- 能为抓取、放置验证和失败恢复提供稳定结果；
- 有仿真与实机泛化实验和失败案例分析。

### 7.3 语音与指令交互

**职责：**把操作人员的自然语言转换为结构化任务，并提供必要反馈。

**当前基础：**已具备 Silero VAD、SenseVoice ASR、LLM 规划入口和 TTS 文件生成。

**L1：**

- 能稳定录制并识别一条固定指令；
- 能得到可用文本。

**L2：**

- 能提取动作、目标类别、属性和目标格；
- 不同说法可以映射到相同任务结构；
- 能识别缺失信息、歧义和无法理解的指令；
- 有 ASR 和槽位解析准确率；
- 能向用户返回明确的成功、失败或澄清信息。

**L3（可选）：**

- 在工业噪声和不同说话人条件下有系统测试；
- 具有领域词汇适配或语言模型微调；
- 有基线对比、响应时间和错误分析。

### 7.4 世界状态表达

**职责：**解耦感知、规划和执行，维护任务期间可信的环境状态。

**L1：**

- 能保存当前目标物体和目标格。

**L2：**

- 使用统一实例 ID；
- 表达类别、属性、置信度和多坐标系位姿；
- 表达机器人、夹爪、料箱格和任务状态；
- 支持状态更新时间、来源和有效性；
- 感知、Agent 和执行模块不直接依赖彼此内部数据结构。

**L3（可选）：**

- 支持多帧跟踪、状态融合和不确定性；
- 可回放完整任务状态；
- 能分析状态错误如何影响任务结果。

### 7.5 Agent 与任务分解

**职责：**把高层指令和当前世界状态转换为受约束、可执行、可解释的任务序列。

**L1：**

- 能为固定任务输出一条固定 ActionList。

**L2：**

- 能处理多个等价自然语言表达；
- 只能选择系统已注册的能力；
- 每一步具有输入、前置条件和预期结果；
- 能根据执行结果进入不同分支；
- 任务过程具有统一日志和可解释输出。

**L3：**

- 覆盖目标不存在、路径失败、抓取失败和放置失败；
- 有任务序列合理性和执行成功率评测；
- 与固定脚本或其他规划方法进行对比；
- 工作流可迁移到不同物体、料箱格和场景；
- LLM 输出受到 Schema、能力白名单和安全规则约束。

#### 7.5.1 任务序列分解专项方案

赛题 PDF 对任务序列分解的要求不是“把自然语言改写成若干文字步骤”，而是要把
“取某个工具并放到指定位置”转化为机器人可执行的底层动作序列，并在识别、
抓取、放置、摆放失败后重新处理之间形成闭环。该能力直接对应评分表中的三类
得分点：

| PDF 得分点 | 与任务分解的关系 | 本项目证据 |
| --- | --- | --- |
| 方案创新性与完整性 35 分 | 是否完整覆盖感知识别、指令理解、任务分解、执行和失败处理 | 系统闭环图、世界状态、DecisionTree/行为树式恢复流程 |
| 技术实现质量 35 分 | 任务序列分解是否合理，执行成功率是否符合机器人作业逻辑 | 任务序列有效率、端到端成功率、失败恢复成功率、对比实验 |
| 系统集成与工程可用性 20 分 | 是否能在仿真/实机中验证，是否具备扩展性 | Gazebo/MoveIt/RM65-B 接口、批量任务日志、实机验证视频 |
| 文档与展示 10 分 | 报告逻辑是否清楚，视频是否直观展示系统流程 | 技术报告、指标表、失败恢复演示视频 |

因此，任务分解模块采用以下路线：

```text
自然语言指令
→ 结构化任务解析
→ 世界状态查询
→ 能力白名单与约束检查
→ 层级任务分解
→ DecisionTree / BehaviorTree 风格执行图
→ 视觉验证
→ 分类恢复或局部重规划
→ 指标记录与报告素材
```

##### 方案调研与取舍

| 候选方案 | 优点 | 问题 | 在本项目中的定位 |
| --- | --- | --- | --- |
| 固定脚本 ActionList | 稳定、易测、实现快 | 不能处理歧义和失败恢复 | 作为 L1 基线和对照组 |
| 纯 LLM 直接输出动作序列 | 灵活、看起来智能 | 容易幻觉、难验证、失败恢复弱 | 不作为主方案，只用于基线对比 |
| HTN/PDDL 符号规划 | 理论扎实、可验证、可解释 | 建模成本高，机器人异常处理仍需执行层 | 用于抽象任务和能力约束设计 |
| 行为树/DecisionTree | 天然支持条件检查、重试、恢复分支 | 初始树需要人工或 LLM 生成 | 作为主执行表示 |
| LLM + 规则/HTN + 行为树 | 兼顾语义理解、约束和恢复 | 工程复杂度较高 | 作为 L3 目标方案 |

主方案不是追求“最时髦”的纯多 Agent，而是采用**受约束 LLM + 能力图/HTN 思想 +
DecisionTree/BehaviorTree 执行图 + 视觉验证器**。这样既能引用 LLM 任务规划、
HTN 层级分解和行为树失败恢复的理论支撑，也能落到当前项目已实现的
`ActionList`、`DecisionTree`、Tool/Skill、结构化日志和 Robot Bridge 上。

##### 理论支撑与参考工作

技术报告中可按以下脉络组织理论依据：

| 方向 | 可引用观点 | 对本项目的启发 |
| --- | --- | --- |
| LLM 任务规划综述 | LLM 适合做自然语言目标理解、子目标生成和工具选择，但需要外部验证、反思和状态反馈 | LLM 只负责生成候选计划，不直接绕过 Schema、能力白名单和执行器 |
| HTN 层级任务网络 | 复杂任务应由复合任务逐层分解为原子动作，分解方法可复用、可解释 | 将“放入指定格”拆为感知、选择、抓取、验证、放置、再验证 |
| PDDL/符号规划 | 前置条件、效果和约束能提升计划合法性 | 为每个步骤定义输入、前置条件、预期结果和失败类型 |
| 行为树/DecisionTree | Selector/条件节点天然支持局部失败恢复和回退 | 用 DecisionTree 承载重试、恢复分支和终止状态 |
| LLM + BT 机器人工作 | KIOS、BTGenBot、H-AIM 等工作展示了 LLM 生成/辅助生成行为树用于机器人执行的可行性 | L3 阶段把 DecisionTree 恢复逻辑可视化为行为树，增强可解释性 |
| Verifier/视觉闭环 | 执行成功不能只依赖控制器返回，需要环境状态确认 | 抓取后和放置后都使用视觉或夹爪状态验证，失败则重新处理 |

报告参考文献可覆盖：

```text
1. Large Language Models for Planning: A Comprehensive and Systematic Survey.
2. Understanding the Planning of LLM Agents: A Survey.
3. ChatHTN: Interleaving Approximate LLM and Symbolic HTN Planning.
4. Online Learning of HTN Methods for Integrated LLM-HTN Planning.
5. Behavior Tree Generation using Large Language Models for Sequential Manipulation Planning.
6. BTGenBot: Behavior Tree Generation for Robotic Tasks with Lightweight LLMs.
7. H-AIM / EmboTeam: LLM + PDDL + Behavior Tree for hierarchical multi-robot planning.
```

##### 场景定制

赛题典型指令是“帮我把滚柱放到料箱的第三个格子中”。本项目将其定制为工业
分拣入格任务族：

```json
{
  "intent": "pick_and_place_to_bin",
  "object": {
    "category": "roller",
    "attributes": ["silver"],
    "instance_policy": "highest_confidence_reachable"
  },
  "target": {
    "type": "bin_cell",
    "cell_id": 3
  },
  "constraints": {
    "allowed_skills": ["detect", "plan_pick", "pick", "verify_grasp", "plan_place", "place", "verify_place"],
    "max_retries_per_failure": 2,
    "requires_visual_verification": true
  }
}
```

分解后的标准子任务为：

```text
1. 解析指令，得到目标类别、属性和目标格；
2. 感知场景，识别候选物体并转换到机器人坐标系；
3. 根据世界状态选择可达目标实例；
4. 生成抓取计划，检查工作空间、碰撞和夹爪约束；
5. 执行抓取；
6. 视觉或夹爪状态验证是否抓住；
7. 生成放置计划，定位目标料箱格；
8. 执行放置；
9. 视觉验证物体是否在指定格；
10. 若失败，按失败类型进入恢复分支。
```

##### 失败分类与恢复策略

| 失败类型 | 检测依据 | 恢复策略 | 指标 |
| --- | --- | --- | --- |
| 目标不存在 | 感知结果为空或置信度低 | 重新观察；换角度；返回缺失信息 | 目标不存在识别准确率 |
| 多目标歧义 | 多个候选分数接近 | 按可达性/置信度选择；必要时请求澄清 | 歧义解析准确率 |
| 规划失败 | MoveIt/Robot Bridge 返回不可达、碰撞、超时 | 调整抓取位姿；切换候选实例；局部重规划 | 路径规划恢复率 |
| 未抓住 | 夹爪闭合状态异常、物体未随夹爪移动 | 重新检测目标；调整抓取点；重试抓取 | 抓取失败恢复率 |
| 途中掉落 | 抓取后/移动中目标丢失 | 停止当前流程；重新检测；回到抓取分支 | 掉落恢复率 |
| 放错格 | 视觉验证目标不在指定格 | 重新抓取并放置到目标格 | 放置失败恢复率 |
| 姿态异常 | 物体在格内但姿态或边界不满足 | 微调放置或重新放置 | 姿态修正成功率 |

当前代码已具备 `DecisionTree` 的条件分支、重试计数和结构化日志。L2 阶段用
DecisionTree 承载恢复逻辑；L3 阶段如果需要更强的可解释展示，可把相同逻辑导出为
行为树图，用于报告和视频说明。

### 7.6 机器人执行

**职责：**提供稳定的机械臂和夹爪能力，不让上层直接处理控制器细节。

**当前基础：**已具备 RM65-B、Robotiq 2F-85、Gazebo、MoveIt 2、轨迹控制和
夹爪 Action。

**L1：**

- 仿真中可手动规划机械臂并开合夹爪；
- 能完成一次固定位置抓取动作。

**L2：**

- 提供查询状态、移动、开合、抓取、放置和停止接口；
- 能报告规划失败、超时、碰撞、取消和控制器错误；
- Agent 可以通过稳定接口调用，不依赖 RViz 人工操作；
- 仿真可完成脚本化抓取放置。

**L3：**

- 仿真和实机共用上层接口；
- 有工作空间、速度、加速度和安全限制；
- 对多个物体与目标格进行批量测试；
- 有路径规划、抓取、放置成功率和耗时指标；
- 接口和执行日志可复现。

### 7.7 结果验证与恢复

**职责：**使用环境结果判断任务是否真正完成，而不是只相信控制器返回成功。

**L1：**

- 能人工观察抓取或放置结果。

**L2：**

- 自动判断物体是否离开原位置；
- 自动判断物体是否随夹爪移动；
- 自动判断物体是否进入指定格子；
- 能区分未抓住、途中掉落、放错格和姿态异常；
- 能限制重试次数并记录失败原因。

**L3：**

- 能重新感知、重新选择目标和重新规划；
- 针对不同失败类型使用不同恢复策略；
- 有失败检测准确率、恢复成功率和额外耗时；
- 覆盖赛题要求的摆放失败自主处理；
- 有失败案例视频和分析。

### 7.8 系统集成与运行

**职责：**保证所有模块可以可靠组合、部署、观测和复现。

**L1：**

- 各模块可以分别启动；
- 人工操作可以完成一次演示。

**L2：**

- 模块通信接口固定；
- 配置区分仿真和实机；
- 全流程具有统一任务 ID、状态和日志；
- 主要异常不会无提示卡死；
- 有一键或少量命令启动流程。

**L3：**

- 能批量运行任务并自动统计指标；
- 能复现指定随机种子和场景；
- 具备健康检查、超时、取消和清理机制；
- 一次模块失败不会破坏后续任务；
- 提供完整安装、运行和故障排查文档。

### 7.9 仿真到实机

**职责：**复用仿真接口完成真实 RM65-B 与 RGB-D 相机任务。

**L1：**

- 能独立读取真实相机；
- 能独立控制真实机械臂。

**L2：**

- 完成相机内参、外参或手眼标定；
- 统一相机、工作台、料箱和机械臂坐标系；
- 仿真与实机共用任务和执行接口；
- 具备人工确认、工作空间限制和紧急停止。

**L3：**

- 完成代表性工业零件的真实分拣入格；
- 覆盖多物体、指定格和失败重试；
- 有批量任务成功率和定位误差；
- 能说明仿真到实机差异及解决方式；
- 有完整实机演示视频和硬件集成说明。

### 7.10 模型训练与实验

**职责：**形成赛题要求的工业模型微调和有说服力的实验结果。

**L1：**

- 能运行一个公开预训练模型；
- 有少量测试样本。

**L2：**

- 有工业数据集和训练/验证/测试划分；
- 训练配置和模型获取方式可复现；
- 有基线结果和微调后结果；
- 能保存指标、模型和推理结果。

**L3：**

- 提供微调前后精度、速度和泛化对比；
- 至少完成一项消融或模块对比实验；
- 有仿真与实机数据结果；
- 有失败样本分类和改进分析；
- 实验结论能支撑技术报告中的创新点。

### 7.11 强化学习

**职责：**作为增强模块优化决策，不进入基础闭环关键路径。

开始条件：

```text
脚本抓取成功率不低于 80%
场景能够自动重置
物体位姿能够随机化
成功和失败能够自动判定
```

**L1：**

- 定义观测、动作、奖励和终止条件；
- 环境通过基础 Gymnasium 检查；
- 随机策略和脚本策略可以运行。

**L2：**

- 针对目标选择、抓取排序、位姿修正或恢复策略中的一个问题进行训练；
- 与脚本基线进行成功率和效率对比；
- 有训练曲线、评估集和可复现实验。

当前阶段不要求达到 L3，不建议训练完整关节力矩控制或端到端视觉抓取。

### 7.12 文档与展示

**职责：**持续沉淀比赛交付物，而不是最后集中补写。

**L1：**

- 有基本 README、架构说明和启动命令。

**L2：**

- 每个模块有接口、依赖、运行和测试说明；
- 持续保存演示视频、实验表格和失败案例；
- 记录硬件、通信、标定和模型信息。

**L3：**

- 技术报告结构完整、数据充分；
- 演示视频清楚展示从指令到失败恢复的全流程；
- 所有关键结论有实验数据支撑；
- 新环境可以按照文档复现主要结果。

## 8. 模块依赖关系

```text
工业场景与数据
├── 视觉感知与定位
├── 模型训练与实验
└── 强化学习

语音与指令交互
→ Agent 与任务分解

视觉感知与定位
→ 世界状态表达
→ Agent 与任务分解
→ 机器人执行
→ 结果验证与恢复

所有模块
→ 系统集成与运行
→ 仿真到实机
→ 文档与展示
```

优先冻结跨模块接口，再并行推进模块内部实现。

## 9. 评测指标框架

### 感知

- mAP50、mAP50-95；
- 推理 FPS；
- 三维定位平均误差；
- 遮挡和混杂条件下的召回率。

### 语言

- ASR 字错率；
- 指令意图准确率；
- 目标类别槽位准确率；
- 属性和目标格槽位准确率。

### 决策与执行

- 任务序列有效率；
- 路径规划成功率；
- 抓取成功率；
- 放置成功率；
- 失败恢复成功率；
- 端到端任务成功率；
- 平均任务耗时。

其中任务序列分解专项指标定义如下：

| 指标 | 定义 | 对应得分点 |
| --- | --- | --- |
| 指令解析准确率 | 正确抽取动作、目标类别、属性、目标格的指令数 / 总指令数 | 指令理解、技术实现质量 |
| 任务序列有效率 | 通过 Schema、能力白名单、前置条件和安全约束检查的序列数 / 总序列数 | 分解合理性 |
| 任务序列完整率 | 包含感知、选择、抓取、验证、放置、再验证和失败处理的序列数 / 总序列数 | 系统完整性 |
| 机器人逻辑一致率 | 步骤顺序符合“先感知再规划、先抓取验证再放置”的序列数 / 总序列数 | 是否符合机器人作业逻辑 |
| 执行分支覆盖率 | 已覆盖的失败类型数 / 预定义失败类型数 | 失败恢复能力 |
| 首次执行成功率 | 无恢复情况下成功完成任务次数 / 总任务次数 | 基础执行质量 |
| 恢复后成功率 | 经过恢复后最终成功次数 / 触发失败任务次数 | 自主重新处理能力 |
| 恢复收益 | 恢复后端到端成功率 - 无恢复端到端成功率 | 恢复模块贡献 |
| 平均恢复代价 | 恢复任务额外耗时、额外动作数、额外规划次数 | 工程可用性 |
| LLM 计划合规率 | LLM 输出中无需人工修正即可通过约束检查的比例 | 受约束规划可靠性 |

### 任务序列分解实验设计

任务序列分解需要从“方案调研、方案拟定、场景定制、实验检测”形成闭环。
实验分为五组：

| 实验 | 目的 | 方法 | 输出 |
| --- | --- | --- | --- |
| E1 指令理解与结构化 | 证明自然语言能稳定进入任务分解 | 构造同义、缺参、歧义、错误目标等指令集 | 槽位准确率、失败样例 |
| E2 任务序列合理性 | 证明分解结果符合工业作业逻辑 | 对 LLM/规则/混合方案输出做 Schema、能力、前置条件检查 | 有效率、完整率、一致率 |
| E3 执行闭环 | 证明分解结果能驱动机器人执行 | 在 mock、Gazebo、实机三层运行同一任务族 | 成功率、耗时、日志 |
| E4 失败恢复 | 证明系统能发现摆放失败并重新处理 | 注入目标不存在、规划失败、未抓住、放错格等故障 | 召回率、恢复后成功率、恢复收益 |
| E5 消融与对比 | 证明所选方案优于简单方案 | 对比固定 ActionList、纯 LLM ActionList、LLM+DecisionTree、LLM+DecisionTree+视觉验证 | 指标表和结论 |

推荐实验矩阵：

| 维度 | 取值 |
| --- | --- |
| 物体类别 | 滚柱、螺钉、螺母、齿轮、法兰或项目最终选定的 4～6 类 |
| 目标位置 | 料箱 1～9 格，至少覆盖角落、边缘、中心 |
| 场景难度 | 单物体、同类多实例、异类混杂、遮挡、光照变化 |
| 指令类型 | 标准指令、同义表达、属性约束、缺失目标格、错误类别 |
| 失败注入 | 目标缺失、低置信度、路径不可达、抓取失败、放置偏移、放错格 |

每个核心场景在仿真中至少运行 30 次；进入实机阶段后，每个代表性任务至少运行
20 次。实验日志需要保存以下字段，以支撑技术报告中的量化结论：

```json
{
  "task_id": "...",
  "instruction": "...",
  "parsed_intent": {},
  "world_state_before": {},
  "selected_plan": {},
  "workflow_type": "decision_tree",
  "nodes": [],
  "failure_type": null,
  "recovery_attempts": 0,
  "world_state_after": {},
  "success": true,
  "duration_ms": 0
}
```

报告中应至少给出以下对比表：

| 方案 | 序列有效率 | 端到端成功率 | 失败恢复成功率 | 平均耗时 | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 固定 ActionList | 待测 | 待测 | 待测 | 待测 | 稳定但不适应异常 |
| 纯 LLM ActionList | 待测 | 待测 | 待测 | 待测 | 灵活但合规性不足 |
| 规则/HTN ActionList | 待测 | 待测 | 待测 | 待测 | 合规但泛化有限 |
| LLM + DecisionTree | 待测 | 待测 | 待测 | 待测 | 兼顾语义和恢复 |
| LLM + DecisionTree + 视觉验证 | 待测 | 待测 | 待测 | 待测 | 目标 L3 方案 |

### 工程与实机

- 一键部署成功率；
- 仿真与实机接口一致性；
- 日志完整性；
- 重复运行稳定性；
- 实机定位误差和任务成功率。

## 10. 阶段安排原则

提交截止时间为 2026 年 9 月 5 日。排期不只描述“开发什么”，还应描述各模块
要达到的等级。

| 时间 | 阶段目标 |
| --- | --- |
| 7 月 15～21 日 | 冻结接口；工业场景、世界状态和 Robot Bridge 达到 L1 |
| 7 月 22～8 月 4 日 | 视觉、三维定位、机器人执行达到 L2；建立模型训练基线 |
| 8 月 5～18 日 | 仿真闭环达到 L2；结果验证与恢复接近 L2 |
| 8 月 19～28 日 | 核心得分模块向 L3 推进；完成实机闭环和批量实验 |
| 8 月 29～9 月 4 日 | 冻结功能；补齐指标、失败分析、视频、报告和复现材料 |

每周应更新模块等级、验收证据、阻塞关系和风险，而不只更新代码任务数量。

## 11. 近期规划顺序

1. 共同确认模块边界和 L0～L3 定义；
2. 为每个模块指定负责人、当前等级和目标等级；
3. 冻结结构化指令、世界状态、感知结果和机器人执行接口；
4. 将工业场景、视觉、Robot Bridge 和实机标定拆成并行任务；
5. 先让完整闭环达到 L2，再选择最有评分价值的模块冲刺 L3；
6. 强化学习保持独立，不阻塞闭环和实机验证。

## 12. 比赛交付物

最终需要同步准备：

- 完整可运行源代码；
- 模型与模型获取说明；
- 仿真验证视频；
- 真实机械臂与相机验证视频；
- 系统架构和算法技术报告；
- 实验指标与改进对比；
- 环境配置、依赖安装和运行说明；
- 硬件配置、通信接口和标定说明。

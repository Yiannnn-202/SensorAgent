# 工业环境交互型智能体赛题技术路线

本文记录针对根目录赛题
`XH-202607_工业环境下物体感知识别与指令交互型智能体研发.pdf`
形成的阶段性技术安排。

## 1. 赛题核心目标

系统需要完成以下闭环：

```text
自然语言指令
→ 工业物体识别与三维定位
→ 指令理解
→ 作业任务序列分解
→ 机械臂抓取与放置
→ 视觉验证
→ 失败重试
```

典型任务：

```text
“帮我把滚柱放到料箱的第三个格子中”
```

不能只生成文字形式的任务序列。答题要求明确提出识别、抓取、放置以及
摆放失败后的自主检测和重新处理。

## 2. 评分导向

| 评审方向 | 分值 | 项目重点 |
| --- | ---: | --- |
| 方案创新性与完整性 | 35 | 完整覆盖感知、决策、执行，并体现复用性 |
| 技术实现质量 | 35 | 视觉精度、任务合理性、执行成功率、工业模型微调 |
| 系统集成与工程可用性 | 20 | 可复现仿真、实机扩展、真实机械臂与相机案例 |
| 文档与展示 | 10 | 技术报告、实验数据和演示视频 |

当前优先级应为：

```text
完整闭环
> 视觉微调和量化指标
> 实机验证
> 失败恢复
> 强化学习扩展
```

强化学习不是硬性要求，不应阻塞基础闭环。

## 3. 当前项目能力

### 已具备

- Agent、Tool、Skill、ActionList 和 DecisionTree 运行框架；
- 静态规划器和 OpenAI 兼容的 LLM 规划器；
- Silero VAD、SenseVoice ASR 和本地 TTS 文件生成；
- RM65-B 与 Robotiq 2F-85 组合模型；
- Gazebo Sim、MoveIt 2、`ros2_control`；
- 机械臂轨迹控制；
- 双指独立 effort 控制；
- 标准 `GripperCommand` Action 接口；
- Ubuntu 下机械臂运动和夹爪开合验证。

### 尚缺

- 工业 RGB-D 视觉识别与三维定位；
- 工业工具数据集和模型微调；
- Agent 到 ROS 2 的真实 robot Tool；
- 工业 Gazebo 工作台、料箱和混杂物体场景；
- 自动场景重置与批量评测；
- 抓取和放置后的视觉验证；
- 真实失败恢复工作流；
- 实机相机标定和完整闭环；
- Gymnasium 强化学习环境。

### 当前关键边界

```text
本地语音链 → Agent → mock.pick_place_actionlist

MoveIt / ROS 2 Action → RM65-B + Robotiq 仿真
```

两条链目前可以独立运行，但尚未连接。

## 4. 系统闭环与模块解耦

系统保持以下闭环：

```text
自然语言输入
→ 指令理解
→ 环境感知
→ 世界状态
→ 任务分解
→ 机器人执行
→ 结果验证
→ 失败恢复或任务完成
```

各模块通过稳定的输入输出接口协作，不应直接依赖其他模块内部实现。
低层轨迹规划、碰撞检查和安全限制继续由 MoveIt 2 与机器人运行时负责；
Agent 负责理解任务、选择能力、组织流程和处理执行结果。

## 5. 统一完成度定义

所有模块统一使用 L0～L3 四级完成度，避免使用“做了”“基本完成”等难以验收的
描述。

| 等级 | 名称 | 定义 |
| --- | --- | --- |
| L0 | 骨架 | 只有目录、接口草案、演示占位或概念验证 |
| L1 | 可运行 | 单模块能够在理想条件下跑通一个固定案例 |
| L2 | 可集成 | 接口稳定，可进入系统闭环，具有基础异常处理和测试 |
| L3 | 有竞争力 | 有量化指标、批量实验、复杂场景、失败分析和对比结果 |

比赛主链模块至少达到 L2，直接对应评分项的核心模块应达到 L3。

### 5.1 模块通用验收材料

模块达到 L2 或 L3 时，原则上应同时具备：

```text
明确的职责边界
稳定的输入输出接口
独立运行示例
配置和依赖说明
异常与失败处理
自动化或可重复测试
量化指标
典型成功和失败案例
系统集成方式
演示与报告素材
```

### 5.2 任务安排字段

后续拆分任务时，每个模块至少记录：

```text
负责人
当前等级
目标等级
输入依赖
输出接口
验收指标
截止时间
风险与备选方案
```

## 6. 模块总览

| 模块 | 核心职责 | 当前等级 | 目标等级 | 评分价值 |
| --- | --- | ---: | ---: | --- |
| 工业场景与数据 | 工具、零件、料箱、工作台、仿真和实机数据 | L0 | L3 | 高 |
| 视觉感知与定位 | 类别识别、实例区分、三维定位 | L0 | L3 | 极高 |
| 语音与指令交互 | VAD、ASR、指令结构化、交互反馈 | L1～L2 | L2 | 中 |
| 世界状态表达 | 统一表达物体、坐标、机器人和任务状态 | L0 | L2 | 高 |
| Agent 与任务分解 | 指令到受约束任务序列和执行分支 | L1 | L3 | 极高 |
| 机器人执行 | MoveIt、夹爪、抓取和放置能力接口 | L1～L2 | L3 | 高 |
| 结果验证与恢复 | 抓取/放置验证、失败分类和重试 | L0 | L3 | 极高 |
| 系统集成与运行 | 通信、配置、日志、一键启动、批量评测 | L1 | L3 | 高 |
| 仿真到实机 | 标定、安全、接口复用和真实案例 | L0～L1 | L3 | 极高 |
| 模型训练与实验 | 微调、对比、消融、指标和失败分析 | L0 | L3 | 极高 |
| 强化学习 | 策略优化，不阻塞基础闭环 | L0 | L1～L2 | 可选加分 |
| 文档与展示 | 报告、视频、说明和实验材料 | L1 | L3 | 必须 |

当前等级是阶段性判断，应在实际验收后调整。

## 7. 各模块目标完成度

### 7.1 工业场景与数据

**职责：**为视觉训练、仿真验证、实机实验和强化学习提供统一的任务环境。

**L1：**

- 有工作台、单个工业物体和料箱；
- 可以人工生成场景并完成一次观察；
- 有少量仿真或真实图像。

**L2：**

- 支持多类工具和零件；
- 支持规则摆放、散乱摆放和基础遮挡；
- 场景可以重复生成；
- 数据具有类别、实例和位置标注；
- 仿真与实机类别定义一致。

**L3：**

- 具有仿真和真实 RGB-D 数据；
- 支持位置、朝向、光照、背景和遮挡随机化；
- 数据集划分、生成和标注流程可复现；
- 包含复杂混杂场景和代表性失败场景；
- 可服务模型训练、系统测试和批量评测。

### 7.2 视觉感知与定位

**职责：**回答“场景里有什么、目标是哪一个、它在什么位置”。

**L1：**

- 能识别一个固定类别；
- 能在理想环境中输出二维检测结果。

**L2：**

- 支持多个工业类别和同类实例；
- 能结合深度信息输出三维位置；
- 能转换到统一机器人坐标系；
- 输出置信度并处理无目标、深度缺失等异常。

**L3：**

- 在混杂、遮挡和不同光照条件下保持竞争力；
- 有 mAP、FPS、三维定位误差和召回率；
- 完成工业数据微调，并提供微调前后对比；
- 能为抓取、放置验证和失败恢复提供稳定结果；
- 有仿真与实机泛化实验和失败案例分析。

### 7.3 语音与指令交互

**职责：**把操作人员的自然语言转换为结构化任务，并提供必要反馈。

**当前基础：**已具备 Silero VAD、SenseVoice ASR、LLM 规划入口和 TTS 文件生成。

**L1：**

- 能稳定录制并识别一条固定指令；
- 能得到可用文本。

**L2：**

- 能提取动作、目标类别、属性和目标格；
- 不同说法可以映射到相同任务结构；
- 能识别缺失信息、歧义和无法理解的指令；
- 有 ASR 和槽位解析准确率；
- 能向用户返回明确的成功、失败或澄清信息。

**L3（可选）：**

- 在工业噪声和不同说话人条件下有系统测试；
- 具有领域词汇适配或语言模型微调；
- 有基线对比、响应时间和错误分析。

### 7.4 世界状态表达

**职责：**解耦感知、规划和执行，维护任务期间可信的环境状态。

**L1：**

- 能保存当前目标物体和目标格。

**L2：**

- 使用统一实例 ID；
- 表达类别、属性、置信度和多坐标系位姿；
- 表达机器人、夹爪、料箱格和任务状态；
- 支持状态更新时间、来源和有效性；
- 感知、Agent 和执行模块不直接依赖彼此内部数据结构。

**L3（可选）：**

- 支持多帧跟踪、状态融合和不确定性；
- 可回放完整任务状态；
- 能分析状态错误如何影响任务结果。

### 7.5 Agent 与任务分解

**职责：**把高层指令和当前世界状态转换为受约束、可执行、可解释的任务序列。

**L1：**

- 能为固定任务输出一条固定 ActionList。

**L2：**

- 能处理多个等价自然语言表达；
- 只能选择系统已注册的能力；
- 每一步具有输入、前置条件和预期结果；
- 能根据执行结果进入不同分支；
- 任务过程具有统一日志和可解释输出。

**L3：**

- 覆盖目标不存在、路径失败、抓取失败和放置失败；
- 有任务序列合理性和执行成功率评测；
- 与固定脚本或其他规划方法进行对比；
- 工作流可迁移到不同物体、料箱格和场景；
- LLM 输出受到 Schema、能力白名单和安全规则约束。

### 7.6 机器人执行

**职责：**提供稳定的机械臂和夹爪能力，不让上层直接处理控制器细节。

**当前基础：**已具备 RM65-B、Robotiq 2F-85、Gazebo、MoveIt 2、轨迹控制和
夹爪 Action。

**L1：**

- 仿真中可手动规划机械臂并开合夹爪；
- 能完成一次固定位置抓取动作。

**L2：**

- 提供查询状态、移动、开合、抓取、放置和停止接口；
- 能报告规划失败、超时、碰撞、取消和控制器错误；
- Agent 可以通过稳定接口调用，不依赖 RViz 人工操作；
- 仿真可完成脚本化抓取放置。

**L3：**

- 仿真和实机共用上层接口；
- 有工作空间、速度、加速度和安全限制；
- 对多个物体与目标格进行批量测试；
- 有路径规划、抓取、放置成功率和耗时指标；
- 接口和执行日志可复现。

### 7.7 结果验证与恢复

**职责：**使用环境结果判断任务是否真正完成，而不是只相信控制器返回成功。

**L1：**

- 能人工观察抓取或放置结果。

**L2：**

- 自动判断物体是否离开原位置；
- 自动判断物体是否随夹爪移动；
- 自动判断物体是否进入指定格子；
- 能区分未抓住、途中掉落、放错格和姿态异常；
- 能限制重试次数并记录失败原因。

**L3：**

- 能重新感知、重新选择目标和重新规划；
- 针对不同失败类型使用不同恢复策略；
- 有失败检测准确率、恢复成功率和额外耗时；
- 覆盖赛题要求的摆放失败自主处理；
- 有失败案例视频和分析。

### 7.8 系统集成与运行

**职责：**保证所有模块可以可靠组合、部署、观测和复现。

**L1：**

- 各模块可以分别启动；
- 人工操作可以完成一次演示。

**L2：**

- 模块通信接口固定；
- 配置区分仿真和实机；
- 全流程具有统一任务 ID、状态和日志；
- 主要异常不会无提示卡死；
- 有一键或少量命令启动流程。

**L3：**

- 能批量运行任务并自动统计指标；
- 能复现指定随机种子和场景；
- 具备健康检查、超时、取消和清理机制；
- 一次模块失败不会破坏后续任务；
- 提供完整安装、运行和故障排查文档。

### 7.9 仿真到实机

**职责：**复用仿真接口完成真实 RM65-B 与 RGB-D 相机任务。

**L1：**

- 能独立读取真实相机；
- 能独立控制真实机械臂。

**L2：**

- 完成相机内参、外参或手眼标定；
- 统一相机、工作台、料箱和机械臂坐标系；
- 仿真与实机共用任务和执行接口；
- 具备人工确认、工作空间限制和紧急停止。

**L3：**

- 完成代表性工业零件的真实分拣入格；
- 覆盖多物体、指定格和失败重试；
- 有批量任务成功率和定位误差；
- 能说明仿真到实机差异及解决方式；
- 有完整实机演示视频和硬件集成说明。

### 7.10 模型训练与实验

**职责：**形成赛题要求的工业模型微调和有说服力的实验结果。

**L1：**

- 能运行一个公开预训练模型；
- 有少量测试样本。

**L2：**

- 有工业数据集和训练/验证/测试划分；
- 训练配置和模型获取方式可复现；
- 有基线结果和微调后结果；
- 能保存指标、模型和推理结果。

**L3：**

- 提供微调前后精度、速度和泛化对比；
- 至少完成一项消融或模块对比实验；
- 有仿真与实机数据结果；
- 有失败样本分类和改进分析；
- 实验结论能支撑技术报告中的创新点。

### 7.11 强化学习

**职责：**作为增强模块优化决策，不进入基础闭环关键路径。

开始条件：

```text
脚本抓取成功率不低于 80%
场景能够自动重置
物体位姿能够随机化
成功和失败能够自动判定
```

**L1：**

- 定义观测、动作、奖励和终止条件；
- 环境通过基础 Gymnasium 检查；
- 随机策略和脚本策略可以运行。

**L2：**

- 针对目标选择、抓取排序、位姿修正或恢复策略中的一个问题进行训练；
- 与脚本基线进行成功率和效率对比；
- 有训练曲线、评估集和可复现实验。

当前阶段不要求达到 L3，不建议训练完整关节力矩控制或端到端视觉抓取。

### 7.12 文档与展示

**职责：**持续沉淀比赛交付物，而不是最后集中补写。

**L1：**

- 有基本 README、架构说明和启动命令。

**L2：**

- 每个模块有接口、依赖、运行和测试说明；
- 持续保存演示视频、实验表格和失败案例；
- 记录硬件、通信、标定和模型信息。

**L3：**

- 技术报告结构完整、数据充分；
- 演示视频清楚展示从指令到失败恢复的全流程；
- 所有关键结论有实验数据支撑；
- 新环境可以按照文档复现主要结果。

## 8. 模块依赖关系

```text
工业场景与数据
├── 视觉感知与定位
├── 模型训练与实验
└── 强化学习

语音与指令交互
→ Agent 与任务分解

视觉感知与定位
→ 世界状态表达
→ Agent 与任务分解
→ 机器人执行
→ 结果验证与恢复

所有模块
→ 系统集成与运行
→ 仿真到实机
→ 文档与展示
```

优先冻结跨模块接口，再并行推进模块内部实现。

## 9. 评测指标框架

### 感知

- mAP50、mAP50-95；
- 推理 FPS；
- 三维定位平均误差；
- 遮挡和混杂条件下的召回率。

### 语言

- ASR 字错率；
- 指令意图准确率；
- 目标类别槽位准确率；
- 属性和目标格槽位准确率。

### 决策与执行

- 任务序列有效率；
- 路径规划成功率；
- 抓取成功率；
- 放置成功率；
- 失败恢复成功率；
- 端到端任务成功率；
- 平均任务耗时。

### 工程与实机

- 一键部署成功率；
- 仿真与实机接口一致性；
- 日志完整性；
- 重复运行稳定性；
- 实机定位误差和任务成功率。

## 10. 阶段安排原则

提交截止时间为 2026 年 9 月 5 日。排期不只描述“开发什么”，还应描述各模块
要达到的等级。

| 时间 | 阶段目标 |
| --- | --- |
| 7 月 15～21 日 | 冻结接口；工业场景、世界状态和 Robot Bridge 达到 L1 |
| 7 月 22～8 月 4 日 | 视觉、三维定位、机器人执行达到 L2；建立模型训练基线 |
| 8 月 5～18 日 | 仿真闭环达到 L2；结果验证与恢复接近 L2 |
| 8 月 19～28 日 | 核心得分模块向 L3 推进；完成实机闭环和批量实验 |
| 8 月 29～9 月 4 日 | 冻结功能；补齐指标、失败分析、视频、报告和复现材料 |

每周应更新模块等级、验收证据、阻塞关系和风险，而不只更新代码任务数量。

## 11. 近期规划顺序

1. 共同确认模块边界和 L0～L3 定义；
2. 为每个模块指定负责人、当前等级和目标等级；
3. 冻结结构化指令、世界状态、感知结果和机器人执行接口；
4. 将工业场景、视觉、Robot Bridge 和实机标定拆成并行任务；
5. 先让完整闭环达到 L2，再选择最有评分价值的模块冲刺 L3；
6. 强化学习保持独立，不阻塞闭环和实机验证。

## 12. 比赛交付物

最终需要同步准备：

- 完整可运行源代码；
- 模型与模型获取说明；
- 仿真验证视频；
- 真实机械臂与相机验证视频；
- 系统架构和算法技术报告；
- 实验指标与改进对比；
- 环境配置、依赖安装和运行说明；
- 硬件配置、通信接口和标定说明。

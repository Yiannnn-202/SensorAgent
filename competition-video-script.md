# SensorAgent 比赛视频介绍文案初稿

> 面向原始赛题 `competition-brief.pdf` 的介绍稿。当前版本按“选题背景、理论支撑、开发路径、系统演示、项目成果”五部分组织，可根据最终视频素材和实机完成情况删减。

---

## 介绍引入

**面向工业现场，贯通感知、理解、执行与恢复全过程。**

评委老师好！我们的作品是 **工业环境下的多模态指令交互与具身自主作业智能体系统**。

接下来，我将从 **选题背景、系统架构、系统演示和项目成果** 四个方面进行介绍。

---

## 一、选题背景

机器自主作业的运用可追溯至二十世纪中后期，随着多模态感知与具身智能技术的发展，开发重点已从“流程自动化”转向“系统智能化”。

然而，现有方案仍然依赖人工任务规划，在工业场景下缺乏高精度自主感知、自主决策能力，这极大限制了智能体系统在复杂作业环境与自然语言交互条件下的运用。

2025年8月，国务院《关于深入实施“人工智能+”行动的意见》明确指出，当代工业生产，需要增强智能感知与决策执行能力。

针对上述现状，本作品围绕工业场景构建“感知-决策-执行”智能体原型，串联可配置的视觉、指令理解、任务工作流和机器人执行接口。其创新价值在于：提升工业指令交互稳定性、以多模态感知支撑可解释决策，并搭建可复用的失败恢复框架。

当前视觉和流程代码已支持工业场景实验，并具有仿真与真机适配接口。最终视频只能展示已经保存的模型指标、仿真结果和经现场确认的真机素材；未完成的工厂或批量验收不得表述为既成结果。

## 二、系统架构

在架构设计上，我们以目标为导向，基于感知、决策、执行三层式智能体结构进行模块划分，并联合调试优化，保证智能体在实际作业中“看得见、听得懂、自主决策、失败自行恢复”。

在感知层，系统支持本地语音输入、开放词汇视觉检测、RGB-D 三维定位和视觉结果验证。
在语音感知流程中，我们基于本地部署的 Silero VAD 和 SenseVoice 实现自然语言捕捉。工业噪声和热词适配效果应以实测记录为准。
视觉感知流程以 Grounding DINO、SAM 2 和深度几何计算为核心，识别工业零件并输出三维坐标。数据采集、微调和指标仅展示已有实验产物，不以代码入口代替结果。

在决策层，系统实现了 AgentRuntime、Planner、ActionList 和 DecisionTree。静态规划器用于稳定基线测试，OpenAI 兼容 LLM 规划器用于自然语言任务解析。所有 LLM 输出都经过目标白名单和输入校验，确保任务分解结果只会进入系统允许的执行流程。

在执行层，系统将高层 Skill 与底层 Tool 解耦。高层包括 `robot.pick`、`robot.place`、`robot.verify_grasp` 和 `robot.verify_place` 等技能；底层包括机械臂关节运动、笛卡尔位姿运动、直线运动、夹爪开合、状态查询、目标格解析和停止等工具。

为了兼顾仿真验证和真机适配，我们将 Python 3.12 的 Agent 进程与 ROS 2 Humble 的机器人运行时分离。仿真 bridge 连接 MoveIt 2、Gazebo 和 Robotiq；硬件 bridge 连接外部 RM65、OmniPicker 和相机 ROS 服务。两条路径复用上层接口，但真机运动默认关闭且仍需现场验收。

此外，项目还建立了统一的配置、JSON 契约、结构化日志和测试体系。不同配置文件可以切换 mock、本地音频、仿真机器人和视觉模型路径；任务执行过程会记录 task_id、工具调用、节点结果、失败类型和恢复尝试，为后续批量实验、报告分析和演示视频提供证据。

## 三、系统演示

这是系统的作业平台：

硬件演示可使用 RM65-B 机械臂与 OmniPicker 夹爪；替换设备需要实现并验证相同的机器人控制接口，不能仅凭抽象接口宣称已经迁移成功。

在运行测试时，我们基于gazebo搭建仿真作业场景。

在终端中运行shell脚本一键启动系统。

任务一。。。

任务二。。。

任务三。。。

如已有现场确认素材，可展示真实机械臂与 RGB-D 相机的单物体抓取测试，并明确其范围。

展示一。。。

其他更多展示。。。

## 四、项目成果

本团队在边缘运算与感知领域经验丰富。。。

如获得可公开核查的试用记录，可在此处补充企业、场景、次数、成功率和授权素材；否则删除本段，不以计划或口头反馈代替验收证据。

在社会价值方面，本作品探索工业生产中的指令交互、感知与作业自动化路径。大规模部署价值仍取决于安全、可靠性、成本和现场验收，不应在缺少证据时作成熟部署表述。

未来，我们将在更多样化的工业场景下强化本作品的性能，并在更加复杂的硬件平台上测试作业效果，经过技术迭代扩展为工业通用具身智能框架。

以上便是本作品全部汇报内容，恳请评委老师批评、指正！
